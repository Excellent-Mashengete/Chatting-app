import 'package:flutter/material.dart';
import 'package:chattingapp/model/models.dart';
import 'package:chattingapp/service/chats_users_api_service.dart';

class ChatProvider extends ChangeNotifier {
  // final MessageApiClient _apiService = MessageApiClient();
  // List _chatResponse = <ChatResponseModel>[];
  // bool _isLoading = false;

  // List get chatResponse => _chatResponse;
  // bool get isLoading => _isLoading;

  // getChats() async {
  //   _isLoading = true;
  //   notifyListeners();
  //   _chatResponse = (await _apiService.getAllChats()) as List;
  //   _isLoading = false;
  //   notifyListeners();
  // }
}
